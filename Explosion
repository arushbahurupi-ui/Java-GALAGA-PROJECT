class Explosion extends Sprite {  //Arush
   Explosion(double startX, double startY, double scale) {
      super(startX, startY, SpriteLibrary.Test, 16); // Sprite initialisieren
      scale(scale);
   }
}
